import React from 'react';
import '../styles/projects.scss';

const Projects = () => (
  <section className="projects">
    <h2>Projects</h2>
    <div className="projects-container">
      {/* 3D previews and project descriptions */}
    </div>
  </section>
);

export default Projects;